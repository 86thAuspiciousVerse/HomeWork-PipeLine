[project]
name = "homework-pipeline"
version = "0.3.0"
description = "Claude Code plugin prototype for verifiable homework delivery pipelines"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "pydantic>=2.0,<3",
  "PyYAML>=6.0",
]

[tool.uv]
package = false
